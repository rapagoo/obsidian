# 예외 처리는 왜 필요한가?

## 한 줄 설명

예외 처리는 실행 중 예상 가능한 오류가 발생했을 때 프로그램 전체가 즉시 멈추지 않도록 대응하는 방법이다.

## 왜 필요한가

사용자는 숫자 대신 문자를 입력하거나 0으로 나눌 수 있다. 오류의 종류를 알고 있다면 안내 메시지를 보여 주고 정리 작업을 수행할 수 있다.

## 핵심 코드

```python
try:
    num1 = int(input("첫 번째 정수 입력: "))
    num2 = int(input("두 번째 정수 입력: "))
    print(num1 / num2)
except ZeroDivisionError:
    print("0으로 나눌 수 없습니다")
except ValueError:
    print("숫자를 입력해주세요")
finally:
    print("프로그램 종료")
```

## 꼭 기억할 점

- 가능한 오류 종류를 알고 있다면 빈 `except:`보다 구체적인 예외 클래스를 사용한다.
- `finally`는 예외 발생 여부와 관계없이 실행된다.
- 예외 처리는 오류를 숨기는 장치가 아니라 예상 가능한 상황을 복구하는 장치다.
- PySide6 셀의 `SystemExit: 0`은 이벤트 루프가 정상 종료되며 Jupyter에 표시될 수 있는 값이다.

## 관련 노트

- [[with open은 왜 사용하는가]]
- [[API 요청과 응답은 어떻게 동작하는가]]

## 출처

- `day0721.ipynb`
- `day0724_2.ipynb`
- `인공지능_프로그래밍_5일차_온디1기.pdf`, 10~24쪽

