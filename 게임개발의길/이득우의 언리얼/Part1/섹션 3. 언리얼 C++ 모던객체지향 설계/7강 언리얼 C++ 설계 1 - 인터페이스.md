인터페이스란 객체가 반드시 구현해야 할 행동을 지정하는 데 활용됨
언리얼 엔진도 인터페이스를 지원한다
월드에 배치되는 모든 오브젝트는 액터라고 한다.
움직이는 오브젝트에 대해서는 액터를 상속받은 pawn을 정의한다
pawn을 정의할때 길찾기 시스템을 반드시 사용하면서 움직여야 한다는 조건을 달고 싶을때는 인터페이스를 사용해서 pawn을 설계해 준다.

`virtual void DoLesson() = 0;` 이런 식으로 하면 구현 강제

## 정리
언리얼 인터페이스는 클래스가 반드시 구현해야 하는 기능을 지정하는데 사용함
C++은 기본적으로 다중상속을 지원하지만, 언리얼 C++의 인터페이스를 사용해 가급적 축소된 다중상속의 형태로 구현하는 것이 향후 유지보수에 도움이 된다.
언리얼 C++ 인터페이스는 두 개의 클래스를 생성한다.
언리얼 C++ 인터페이스는 추상 타입으로 강제되지 않고, 내부에 기본 함수를 구현할 수 있다.
