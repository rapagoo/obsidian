헤더 파일에 변경이 발생하면 에디터를 끄고 비주얼 스튜디오에서 컴파일한다
소스 파일에만 변경이 발생하면 라이브 코딩으로 컴파일한다
엔진이 제공하는 부모클래스를 상속받아서 재구현하는 경우에는 대부분 엔진이 미리 작업해둔 코드를 실행해줘야 한다
가장 많이 하는 실수가 상위 클래스의 가상 함수를 실행하는 것을 깜박하는 것이다
Super 키워드와 함수 이름을 써서 상속받는 것이 기본이다

로그를 남기는 방법
UE_LOG() 매크로 함수를 사용한다
언리얼 엔진에서는 string을 다룰 때 TEXT 매크로를 사용한다

GameInstance란 컨텐츠를 담는 어플리케이션의 뼈대이다
